apiVersion: apps/v1
kind: Deployment # type of resource
metadata:
  name: tts-api
  labels:
    app: tts-api
    env: production
spec:
  #replicas: {{.Values.deployment.replicas}}
  replicas: 2
  selector:
    matchLabels:
      app: tts-api
  template:
    metadata:
      #annotations:
        #prometheus.io/port: "5000"
        #prometheus.io/path: "/metrics"
        #prometheus.io/scrape: "true"
      labels:
        app: tts-api
    spec:
      containers:
        - name: tts-api
          image: melekelloumi/text_to_speech_api:latest
          #image: {{.Values.image.ttapi}}
          imagePullPolicy: Always
          resources:
          ports:
            #- containerPort: {{.Values.http.ttport}}
            - containerPort: 8082
      restartPolicy: Always