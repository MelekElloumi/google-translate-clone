apiVersion: apps/v1
kind: Deployment
metadata:
  name: tt-api
  labels:
    app: tt-api
    env: production
spec:
  replicas: {{.Values.replicaCountApi}}
  selector:
    matchLabels:
      app: tt-api
  template:
    metadata:
      annotations:
        admission.datadoghq.com/python-lib.version: v1.7.1
        prometheus.io/port: "8081"
        prometheus.io/path: "/metrics"
        prometheus.io/scrape: "true"
      labels:
        app: tt-api
    spec:
      containers:
        - name: tt-api
          image: {{.Values.image.ttapi}}
          imagePullPolicy: Always
          resources:
          ports:
            - containerPort: {{.Values.http.ttport}}
          env:
            - name: DD_PROFILING_ENABLED
              value: "true"
            - name: DD_LOGS_INJECTION
              value: "true"
      restartPolicy: Always